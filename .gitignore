# Dependencies
node_modules/

# Nested repo (avoid "does not have a commit checked out")
silver-telegram/

# Env (keep secrets out of repo)
.env
*.local

# Build / cache
dist
.vite

# OS / editor
.DS_Store
*.log
